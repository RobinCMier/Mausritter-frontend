//imports
/*TO DO
- create store for user
- scavenge heartapp for parts
- make a THUNK to post request a login to the right endpoint
- dispatch info to store user slice
- if login success, redirect to homepage
*/
//default function
export default function Login() {
  return <div>HI FROM LOGIN</div>;
}
